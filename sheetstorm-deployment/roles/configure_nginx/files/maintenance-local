server {

    listen 80 default_server;
    listen [::]:80 default_server;

    error_page 503 /503.html;
    location = /503.html {
        root /var/www/html/;
        internal;
    }

    location / {
        return 503;
    }
}

